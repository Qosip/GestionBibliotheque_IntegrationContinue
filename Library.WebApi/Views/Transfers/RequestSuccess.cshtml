@model Library.Application.RequestTransferResult

@{
    ViewData["Title"] = "Transfert demandé";
}

<div class="card">
    <div class="card-body">
        <h2 class="card-title mb-3">Transfert demandé</h2>

        <p>Exemplaire mis en transfert. BookCopyId : <strong>@Model.BookCopyId</strong></p>

        <div class="mt-3">
            <a href="/Transfers/Request" class="btn btn-secondary me-2">Demander un autre transfert</a>
            <a href="/Home/Index" class="btn btn-link">Retour à l'accueil</a>
        </div>
    </div>
</div>
