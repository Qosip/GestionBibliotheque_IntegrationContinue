@model Library.Application.RequestTransferResult

@{
    ViewData["Title"] = "Transfert demandé";
}

<div class="card">
    <div class="card-body">
        <h2 class="card-title mb-3">Transfert demandé</h2>

        <p>Exemplaire mis en transfert. BookCopyId :</p>
        <pre class="bg-light p-2">@Model.BookCopyId</pre>

        <a href="/Home/Index" class="btn btn-primary">Retour à l'accueil</a>
        <a href="/Transfers/Request" class="btn btn-outline-secondary ms-2">Nouveau transfert</a>
    </div>
</div>
